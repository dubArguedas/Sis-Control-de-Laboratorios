@page "/laboratorio-crear/{Torre?}" 
@layout AdminLayout
@rendermode InteractiveServer
@inject NavigationManager NavManager

<SCLAB_Client.Components.Pages.Admin.Componentes.LaboratorioForm 
    LaboratorioId="0" 
    Torre="@Torre" />

@code {
    [Parameter]
    public string? Torre { get; set; } // opcional
}
